spring:
  application:
    name: consumer

eureka:
  client:
    service-url:
      fetch-registry: true
      register-with-eureka: true
      defaultZone: http://localhost:8888/eureka

server:
  port: 8080